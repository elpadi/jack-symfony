{% extends 'base.html.twig' %}

{% block content %}
<main id="page-content">
    <div class="text-content"><h1>{{ page.title }}</h1></div>
    <div class="text-content">
        {{ include(template_from_string(page.content)) | raw }}
    </div>
</main>
{% endblock %}
