<img class="lazy" data-src="{{ img.src }}" data-srcset="{{ img.srcset }}" alt="{{ img.title ?? '' }}" width="{{ img.width }}" height="{{ img.height }}">
<noscript>
    <img itemprop="photo" src="{{ img.src }}" alt="{{ img.title ?? '' }}" width="{{ img.width }}" height="{{ img.height }}">
</noscript>
