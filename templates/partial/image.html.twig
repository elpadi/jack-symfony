<img
    src="{{ img.src }}"
    srcset="{{ img.srcset }}"
    alt="{{ img.title ?? '' }}"
    width="{{ img.width }}"
    height="{{ img.height }}"
    {% if loading is defined %}
    loading="{{ loading }}"
    {% endif %}
 >
